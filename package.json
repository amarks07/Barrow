{
  "name": "barrow-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:web": "npm run export:web --workspace=apps/mobile && npm run build --workspace=apps/web"
  }
}
