// No OS-level notification tray on web — the in-app Focus flow and PWA
// badge (if added later) cover the same "workout in progress" signal.
export async function refreshFocusNotification() {}

export async function cancelFocusNotification() {}
